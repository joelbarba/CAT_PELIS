// credits module (independent module)
define(function(credits) {
        return {
            getCredits: function() {
              console.log("Function : getCredits");
              var credits = 1;
              return credits;
            }
        };
    });